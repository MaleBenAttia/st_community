requests>=2.31.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
markdownify>=0.11.0
flask>=3.0.0

# Serveur WSGI de production (au lieu du serveur de dev Flask)
waitress>=3.0.0

# Note : les dépendances frontend (React, Vite) sont gérées par npm
# (voir frontend/package.json). Le bundle buildé est versionné dans
# Dashboard/static/floating/ — aucune étape npm n'est requise en production.